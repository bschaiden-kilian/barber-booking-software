export type Service = {
  id: string;
  name: string;
  description: string;
  durationMinutes: number;
  priceEuro: number;
};

export type ServiceDTO = {
  id: string;
  name: string;
  description: string;
  durationMinutes: number;
  priceEuro: number;
};
